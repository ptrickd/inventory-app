//Material UI
import { css } from "@mui/material-pigment-css";

export const rootStyle = css({});

export const dialogContentStyle = css({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});

export const messageTypoStyle = css({});

export const buttonStyle = css({ width: "100%", maxWidth: 50 });
