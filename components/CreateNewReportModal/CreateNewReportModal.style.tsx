//Material Ui
import { css } from "@mui/material-pigment-css";

export const rootStyle = css({
  // margin: theme.spacing(4),
  // padding: theme.spacing(4),
});

export const buttonStyle = css({ marginTop: 10 });
